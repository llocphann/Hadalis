pragma ComponentBehavior: Bound

import qs.modules.common.widgets

// Compatibility alias for legacy callers that still import qs.modules.pill.
// The actual island surface implementation is owned by common/widgets.
RicelinSurface {}
